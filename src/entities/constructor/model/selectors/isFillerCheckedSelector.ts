// import {RootState} from "../../../../app/store";
// import {createSelector} from "@reduxjs/toolkit";
//
// const isFillerChecked = (state: RootState) => state.constructorReducer.constructor.fillers && state.constructorReducer.constructor.fillers.isChecked;
//
// export const isFillerCheckedSelector = createSelector(
//   [ isFillerChecked ],
//   (isFillerCheckedState) => {
//
//     return isFillerCheckedState;
//   }
// );
